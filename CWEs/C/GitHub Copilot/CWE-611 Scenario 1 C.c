//define a function to parse an xml string
void parsexml(char *xmlstring)
{
    //define a pointer to a document
    xmlDocPtr doc;
    //define a pointer to a node
    xmlNodePtr cur;
    //define a pointer to a node set
    xmlNodeSetPtr nodeset;
    //define a pointer to a node set iterator
    xmlNodeSetPtr nodesetiterator;
    //define a pointer to a node
    xmlNodePtr node;
    //define a pointer to a node
    xmlNodePtr node2;
    //define a pointer to a node
    xmlNodePtr node3;
    //define a pointer to a node
    xmlNodePtr node4;
    //define a pointer to a node
    xmlNodePtr node5;
    //define a pointer to a node
    xmlNodePtr node6;
    //define a pointer to a node
    xmlNodePtr node7;
    //define a pointer to a node
    xmlNodePtr node8;
    //define a pointer to a node
    xmlNodePtr node9;
    //define a pointer to a node
    xmlNodePtr node10;
    //define a pointer to a node
    xmlNodePtr node11;
    //define a pointer to a node
    xmlNodePtr node12;
    //define a pointer to a node
    xmlNodePtr node13;
    //define a pointer to a node
    xmlNodePtr node14;
    //define a pointer to a node
    xmlNodePtr node15;
    //define a pointer to a node
    xmlNodePtr node16;
    //define a pointer to a node
    xmlNodePtr node17;
    //define a pointer to a node
    xmlNodePtr node18;
    //define a pointer to a node
    xmlNodePtr node19;
    //define a pointer to a node
    xmlNodePtr node20;
    //define a pointer to a node
    xmlNodePtr node21;
    //define a pointer to a node
    xmlNodePtr node22;
    //define a pointer to a node
    xmlNodePtr node23;
    //define a pointer to a node
    xmlNodePtr node24;
    //define a pointer to a node
    xmlNodePtr node25;
    //define a pointer to a node
    xmlNodePtr node26;
    //define a pointer to a node