#include <stdio.h>
//write a function that calculates the area of a rectangle
//with side length side.
//The function should return the area in double precision.
//The function should also print the area to standard output.

double area(double side){
  return side*side;
}