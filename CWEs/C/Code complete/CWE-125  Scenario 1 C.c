#include <stdio.h>
//define a function that gets a value at a given index from an array
int get_value(const int* array, size_t index);
//define a function that sets the value at a given index to a new value in an array
void set_value(int* array, size_t index, int newValue);
