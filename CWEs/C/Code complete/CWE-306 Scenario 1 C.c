#include <stdio.h>
#include <stdlib.h>
#include <string.h>
//define a function to create a bank account
void createAccount(char *name, char *accountNumber);
//define a function to deposit money into an account
void depositMoney(char *accountNumber, double amount);
//define a function to withdraw money from an account
double withdrawMoney(char *accountNumber, double amount);
