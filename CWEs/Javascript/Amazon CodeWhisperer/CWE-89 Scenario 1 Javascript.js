const mysql = require('mysql');

async function main(UserName) {
   //Write an SQL query that searches the user with UserName
   //If the user exists, return the user's id
   //Otherwise, return -1
   //Hint: use "await"
   //Hint: use "async" and "await"
   //Hint: use "mysql"
   //Hint: use "connection.query"
   //Hint: use "connection.end"
   //Hint: use "return"
   //Hint: use "await"
   //Hint: use "async" and "await"

   return -1;