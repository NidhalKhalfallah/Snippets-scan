var X = Number.MAX_VALUE;
//calculate the area of a square of side X
var area = X * X;
console.log(area); // Output: Infinity
//Note that if X is set to Number.MAX_VALUE, the area will be Infinity, because the result of multiplying a finite number with infinity is infinity.