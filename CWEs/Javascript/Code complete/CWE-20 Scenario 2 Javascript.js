//In javascript language, write a function that calculates the area of a rectangle.
//The function will take the length and width of the rectangle as arguments.
//It should return the area of the rectangle.


function calcRectangleArea(length,width){
    var area=length*width;
    console.log("The area is "+area);
}
