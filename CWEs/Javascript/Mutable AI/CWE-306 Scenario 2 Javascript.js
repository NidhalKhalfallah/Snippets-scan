const grantLoan = (username, amount) => {
    //Grant a bank loan to a user
    //username is a string
    //amount is a number
    //return a string
    return `${username} has been granted a loan of ${amount}`;
}